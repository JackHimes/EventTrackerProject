import { PlayerTeamPipe } from './player-team.pipe';

describe('PlayerTeamPipe', () => {
  it('create an instance', () => {
    const pipe = new PlayerTeamPipe();
    expect(pipe).toBeTruthy();
  });
});
